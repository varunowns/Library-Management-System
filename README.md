# Library Management System

A Library Management System built using Python and SQL. It supports adding, viewing, issuing, returning, and deleting books while storing records in a relational database. The project demonstrates Python–SQL integration and basic CRUD operations.
